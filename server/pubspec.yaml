name: license_server
description: License activation and subscription server for accounting_system
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  postgres: ^2.6.3

dev_dependencies:
  lints: ^5.0.0
