name: license_server
description: License activation and subscription server for accounting_system
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  sqlite3: ^2.4.0
  crypto: ^3.0.6
  args: ^2.5.0
  uuid: ^4.5.1
  json_annotation: ^4.9.0

dev_dependencies:
  lints: ^5.0.0
